#include <stdio.h>
//problem 5
int main() {
    int i, j; 
    scanf("%d%d", &i, &j);

    if (i < j)
        for (; i < j; i++)
            printf("%d, ", i*i);
    else if (i > j)
        for (; i > j; i--)
            printf("%d, ", i*i);

    printf("Reached!\n");
    return 0;
}
