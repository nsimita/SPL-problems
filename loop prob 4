//problem 4
#include<stdio.h>
int main() {
    int i, n;
	double num, avg, sum = 0;
	
	scanf("%d", &n);
	for (i = 1;i <= n;i++) {
		scanf("%lf", &num);
		sum += num;
	}
	avg = sum / n;
	printf("AVG of %d inputs : %lf", n, avg);
	return 0;
}
