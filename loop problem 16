#include<stdio.h>
//problem 16
int main() {
	int   max, n1, n2,GCD;
	scanf("%d%d", &n1, &n2);
	max=n1 > n2 ? n1 : n2;
	for (;;max++) {
		if (max % n1 == 0 && max % n2 == 0) {
			
			break;
		}
	}
	GCD = (n1 * n2) / max;
	printf("GCD : %d\n",  GCD);
	printf("LCM : %d\n",  max);
	return 0;
}
